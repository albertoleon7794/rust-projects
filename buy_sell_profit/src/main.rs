




fn main() {
    println!("Hello, world!");

    pub fn max_profit(prices: Vec<i32>) -> i32 {
    let mut buy = prices[0];
    let mut sell = pri0;
    
        for &price in prices.iter() {
            println!("price {}", price);
            if price < buy {
                buy = price;
            } if else () {} 
        }
        println!("buy {}", buy);
        return 0;
    }

    max_profit([7,1,5,3,6,4].to_vec());

}
