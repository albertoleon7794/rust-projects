# Compiled files
guessing_game/target/

# Remove Cargo.lock from gitignore if creating an executable, keep it for libraries
guessing_game/Cargo.lock

# These are backup files generated by rustfmt
guessing_game/**/*.rs.bk

# MSVC Windows builds of rustc produce these, which store debugging information
guessing_game/**/*.pdb